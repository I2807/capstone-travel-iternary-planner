# The frontend uses same-origin /api requests in development and production.
# For GitHub Pages, set VITE_API_BASE_URL to the separately hosted FastAPI URL.
# Keep Google and weather credentials in backend/.env only.
VITE_API_BASE_URL=
